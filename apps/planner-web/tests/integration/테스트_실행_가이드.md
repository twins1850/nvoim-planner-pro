# 통합 테스트 실행 가이드 (한글)

## 🚀 빠른 시작 (3단계)

### 1단계: 학생 앱 서버 시작

**새 터미널을 열고:**
```bash
cd apps/student
./start-server.sh
```

또는 수동으로:
```bash
cd apps/student
npx serve web-build -l 10001
```

✅ 성공하면 이런 메시지가 나옵니다:
```
🌐 서버 주소: http://localhost:10001
📱 학생 앱: React Native Web
```

### 2단계: 테스트 실행

**원래 터미널에서:**
```bash
cd apps/planner-web
./run-integration-tests.sh
```

또는 직접:
```bash
cd apps/planner-web
npm run test:integration
```

### 3단계: 결과 확인

✅ **성공 시:**
```
Running 4 tests using 1 worker
  4 passed (3.4m)
```

❌ **실패 시:**
- `test-results/` 폴더에서 스크린샷 확인
- 에러 메시지 읽고 문제 파악

## 📊 테스트 내용

### Test 1: 완전한 플로우
- 플래너가 초대코드 생성
- 학생이 코드 입력하여 연결
- 양방향 연결 확인

### Test 2: 잘못된 코드 처리
- 존재하지 않는 코드
- 만료된 코드
- 올바른 에러 메시지 표시

### Test 3: 학생 수 제한
- 5명까지 연결 성공
- 6번째 학생 연결 거부
- 라이선스 제한 정상 작동

### Test 4: 중복 방지
- 같은 이메일로 2번 가입 시도
- "중복확인" 버튼에서 에러 표시
- 데이터베이스에 1개만 저장

## 🎯 테스트 옵션

### 기본 실행
```bash
npm run test:integration
```

### UI 모드 (디버깅 편함)
```bash
npm run test:integration:ui
```
- 브라우저가 열려서 각 테스트 단계를 볼 수 있음
- 테스트를 일시정지하고 DOM을 검사할 수 있음

### 브라우저 표시 모드
```bash
npm run test:integration:headed
```
- 브라우저 창이 보이면서 실행됨
- 자동화 과정을 눈으로 확인 가능

### 특정 테스트만 실행
```bash
npm run test:integration tests/integration/06-invite-code-flow.spec.ts
```

## 🔧 문제 해결

### Q: "학생 앱 서버가 실행되지 않았습니다" 에러
**해결:**
```bash
# 1. 학생 앱 디렉토리로 이동
cd apps/student

# 2. web-build가 있는지 확인
ls web-build/

# 3. 없으면 빌드
npm run build:web

# 4. 서버 시작
npx serve web-build -l 10001
```

### Q: "포트 10001이 이미 사용 중" 에러
**해결:**
```bash
# 1. 실행 중인 프로세스 확인
lsof -ti:10001

# 2. 프로세스 종료
kill $(lsof -ti:10001)

# 3. 다시 서버 시작
npx serve web-build -l 10001
```

### Q: 테스트가 타임아웃됨
**원인:**
- 학생 앱 서버가 느림
- 네트워크 문제
- Supabase 연결 느림

**해결:**
1. 학생 앱 서버 재시작
2. 인터넷 연결 확인
3. 다시 테스트 실행

### Q: 데이터베이스 에러
**해결:**
1. `.env.local` 파일 확인
2. Supabase 환경 변수가 올바른지 확인:
   ```
   SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...
   ```
3. Supabase 대시보드에서 데이터베이스 접속 확인

### Q: 스크린샷 어디서 보나요?
**위치:**
```
apps/planner-web/test-results/
├── integration-06-invite-code-.../
│   ├── test-failed-1.png
│   ├── test-failed-2.png
│   └── error-context.md
```

## 📈 정상 실행 흐름

```
🚀 통합 테스트 실행 준비 중...

📱 학생 앱 서버 상태 확인...
✅ 학생 앱 서버 실행 중 (PID: 9428)

📋 환경 변수 확인...
✅ 환경 변수 파일 존재

🧪 통합 테스트 실행 중...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Running 4 tests using 1 worker

[1/4] Complete flow: planner generates code → student connects
🧪 Starting test...
✅ Test passed

[2/4] Invalid/expired code handling
🧪 Starting test...
✅ Test passed

[3/4] Student limit enforcement (5 max)
🧪 Starting student limit enforcement test
✅ All 5 students connected successfully
✅ 6th student connection rejected
✅ Test passed

[4/4] Duplicate student connection prevention
🧪 Starting duplicate connection prevention test
🔄 Creating fresh browser context...
✅ First connection succeeded
🔍 Duplicate check button clicked
✅ Duplicate prevented
✅ Test passed

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  4 passed (3.4m)

✅ 테스트 실행 완료!
```

## 📚 추가 정보

### 개발 현황 문서
```bash
# Phase 10 섹션 참조
cat ../../DEVELOPMENT_STATUS.md | grep -A 100 "Phase 10"
```

### 영문 가이드
```bash
cat tests/integration/README.md
```

### 테스트 코드 보기
```bash
cat tests/integration/06-invite-code-flow.spec.ts
```

### 최근 커밋 확인
```bash
git log --oneline | grep "test(integration)"
```

## 🎉 테스트 통과 축하합니다!

모든 테스트가 통과했다면:
- ✅ 초대코드 시스템이 정상 작동합니다
- ✅ 학생 수 제한이 올바르게 작동합니다
- ✅ 중복 방지 기능이 정상입니다
- ✅ 플래너-학생 연결이 안정적입니다

이제 안심하고 프로덕션에 배포할 수 있습니다! 🚀

## 💡 팁

### 개발 중 자주 테스트하기
```bash
# 파일 감시 모드 (변경 시 자동 재실행)
npm run test:integration -- --watch
```

### 실패한 테스트만 재실행
```bash
npm run test:integration -- --last-failed
```

### 디버그 모드
```bash
# Playwright Inspector 사용
PWDEBUG=1 npm run test:integration
```

---

**궁금한 점이 있으면:**
- README.md (영문 가이드)
- DEVELOPMENT_STATUS.md (Phase 10)
- Claude Code Assistant에게 질문하기

**마지막 업데이트**: 2026년 2월 3일
